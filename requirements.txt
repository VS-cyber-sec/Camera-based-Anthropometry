# Camera-Based Anthropometry — Python Dependencies
# Install with: pip install -r requirements.txt

# Core computer vision
mediapipe>=0.10.0
opencv-python>=4.8.0

# Numerical / data
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Reporting
xlsxwriter>=3.1.0
openpyxl>=3.1.0

# Jupyter notebooks (optional — for analysis notebooks)
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Camera calibration
# (uses cv2.calibrateCamera — already in opencv-python)

# Utilities
tqdm>=4.66.0
